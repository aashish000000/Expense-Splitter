# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration (comma-separated list of allowed origins)
# Leave empty or set to * for development
CORS_ORIGINS=http://localhost:3000

# Database
DB_PATH=./data/database.jsonl
