// const API_BASE_URL = 'http://localhost:3000';
const API_BASE_URL = 'https://expense-splitter-br477lj8k-aashish000000s-projects.vercel.app';

document.querySelector('form').addEventListener('submit', async function(event) {
    event.preventDefault(); // Prevent the default form submission

    const username = document.getElementById('username').value;
    const email = document.getElementById('email').value;
    const password = document.getElementById('password').value;
    const confirmPassword = document.getElementById('confirm-password').value;

    // Check if passwords match
    if (password !== confirmPassword) {
        alert('Passwords do not match!');
        return;
    }

    try {
        const response = await fetch(`${API_BASE_URL}/register`, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({ username, email, password })
        });

        if (response.ok) {
            const result = await response.json();
            alert('Registration successful: ' + result.message);
            window.location.href = 'login.html';
        } else {
            const error = await response.json();
            alert('Registration failed: ' + error.message);
        }
    } catch (err) {
        console.error('Error:', err);
    }
});
[Error] Preflight response is not successful. Status code: 401
[Error] Fetch API cannot load https://expense-splitter-br477lj8k-aashish000000s-projects.vercel.app/register due to access control checks.
[Error] Failed to load resource: Preflight response is not successful. Status code: 401 (register, line 0)
[Error] Error: – TypeError: Load failed
TypeError: Load failed
	(anonymous function) (register.js:36)